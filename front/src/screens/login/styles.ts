import { constants } from "../../constants";

const styles = {
  animation: {
    width: constants.elements.animation,
    height: constants.elements.animation,
  },
  buttonContainer: {
    margin: constants.spacing.big,
    height: constants.size.large,
    justifyContent: "center",
    display: "flex",
  },
  errorContainer: {
    height: constants.size.normal,
  },
};

export default styles;
